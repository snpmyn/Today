<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/bg_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#DC000000"
    android:clickable="true"
    android:focusable="true">
    <!-- 图片（Matrix 控制缩放 / 平移） -->
    <view
        android:id="@+id/iv_image"
        class="com.zsp.today.module.heartbox.ImageViewerOverlay$AccessibleImageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@null"
        android:scaleType="matrix" />
    <!-- 加载指示器 -->
    <ProgressBar
        android:id="@+id/pb_loading"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="center"
        android:indeterminate="true" />
    <!-- 错误提示 -->
    <TextView
        android:id="@+id/tv_error"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:textColor="@android:color/white"
        android:textSize="14sp"
        android:visibility="gone" />
    <!-- 右上角关闭按钮 -->
    <ImageView
        android:id="@+id/iv_close"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="top|end"
        android:layout_margin="8dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:contentDescription="关闭"
        android:focusable="true"
        android:padding="10dp"
        android:src="@drawable/ic_close_cos_24dp" />
    <!-- 底部工具栏（旋转按钮） -->
    <LinearLayout
        android:id="@+id/ll_toolbar"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_gravity="bottom"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="8dp"
        android:paddingEnd="8dp">
        <!-- 左旋 90° -->
        <ImageView
            android:id="@+id/iv_rotate_left"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:contentDescription="向左旋转90度"
            android:focusable="true"
            android:padding="10dp"
            android:src="@drawable/ic_close_cos_24dp" />

        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />
        <!-- 右旋 90° -->
        <ImageView
            android:id="@+id/iv_rotate_right"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:contentDescription="向右旋转90度"
            android:focusable="true"
            android:padding="10dp"
            android:src="@drawable/ic_close_cos_24dp" />
    </LinearLayout>
</FrameLayout>